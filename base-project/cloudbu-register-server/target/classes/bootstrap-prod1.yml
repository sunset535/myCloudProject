eureka:
  instance:
    hostname: registry1.cloudbu
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://registry2.huistore:${server.port}/eureka/

