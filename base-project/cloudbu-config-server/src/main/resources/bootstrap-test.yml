spring:
  profiles:
    active: native,test
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/conf/

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/