server:
  port: 3300

spring:
  profiles:
    active: dev
  application:
    name: si-manage-service
  cloud:
    config:
      label: cloudbu-si-manage
      discovery:
        enabled: true
        service-id: cloudbu-config-server